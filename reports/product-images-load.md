# product_images load

DRY RUN — nothing written. `npm run load:images:write` to apply.

| | rows | photographs |
|---|---:|---:|
| `sole` — the ERP has no picture and cannot be given one | 4 | 4 |
| `gallery` — repainted angles behind the ERP's own | 25 | 63 |
| **total** | **29** | **67** |

823 raw `/product-images/` photographs were NOT written: their white
backdrop does not match the ERP's tile. They become eligible as
normalize-product-bg.py works through them.

  new rows          0
  loader-owned      29  (safe to refresh)
  edited by a human 0  — skipped

Nothing written.
